import { ContactScreen } from "@features/contact";

export const metadata = {
  title: "Contact — Pure Match Halal",
  description:
    "A question, a concern, or just not sure where to start — reach out to Pure Match Halal. You will always speak to a person.",
};

export default function ContactPage() {
  return <ContactScreen />;
}
