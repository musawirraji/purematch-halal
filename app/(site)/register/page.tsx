import { RegisterScreen } from "@features/register";

export const metadata = {
  title: "Begin Your Application — Pure Match Halal",
  description:
    "Begin your application to Pure Match Halal — the first step toward finding your spouse, the halal way. Complete the three-step form and we will reach out personally.",
};

export default function RegisterPage() {
  return <RegisterScreen />;
}
